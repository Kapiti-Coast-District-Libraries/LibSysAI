name: Deploy Vite React App to GitHub Pages

on:
  push:
    branches:
      - main

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install dependencies
        run: npm install
      - name: Generate icons.json
        run: node generateIcons.cjs

      - name: Commit updated icons.json
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
      
          git add icons/icons.json
      
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Auto-update icons.json"
            git push
          fi
          
      - name: Build
        run: npm run build
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}

     # - name: Generate and commit manifest
      #  run: |
      #    node generateManifest.cjs
      #    git config --global user.name "Kapiti-Coast-District-Libraries"
      #    git config --global user.email "Paraparaumumake@gmail.com"
      #    git add manifest.json
      #    git commit -m "Auto-update manifest.json" || echo "No changes to commit"
      #    git push
      #  env:
      #    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
